#include <bits/stdc++.h>
int findDuplicate(vector<int> &nums, int n){
        sort(nums.begin(),nums.end());
        for(int i = 0; i < nums.size() - 1;){
            if(nums[i] == nums[ i + 1 ]){
                return nums[i];
                i = i + 2; 
            }
            else 
                i++;
        }
}
