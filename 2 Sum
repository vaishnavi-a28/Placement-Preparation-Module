string read(int n, vector<int> book, int target)
{
unordered_set<int> s;
    int val;
    for(int i=0;i<book.size();i++)
    {
        val=target-book[i];
        if(s.find(val)!= s.end())
        {
            return "YES";
        }
        else
        {
            s.insert(book[i]);
        }
    }
    return "NO";
}
