#include<bits/stdc++.h>
int subarraysWithSumK(vector < int > a, int k) {
    int xr=0;
    map<int,int>m;
    m[xr]++;
    int cnt=0;
    for(int i=0; i<a.size(); i++){
        xr=xr^a[i];
        int x = xr^k;
        cnt+=m[x];
        m[xr]++;
    }
    return cnt;
}
